CREATE TABLE tbl_post_books (
    id INT PRIMARY KEY AUTO_INCREMENT,
    author VARCHAR(255),
    title VARCHAR(255),
    description VARCHAR(255)
);

drop tables tbl_post_books;

select * from tbl_post_books;
